spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: localhost

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 7.0.0

user-service:
  url: http://localhost:8090